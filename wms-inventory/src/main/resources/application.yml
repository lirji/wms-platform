spring:
  application:
    name: wms-inventory
server:
  address: ${WMS_BIND_ADDRESS:127.0.0.1}
  port: ${WMS_HTTP_PORT:18183}
  max-http-request-header-size: 64KB
management:
  endpoints:
    web:
      exposure:
        include: health,metrics
  metrics:
    distribution:
      percentiles-histogram:
        http.server.requests: true
      percentiles:
        http.server.requests: 0.95,0.99
  endpoint:
    health:
      show-details: never
      group:
        readiness:
          include: readinessState,wmsReadiness
        liveness:
          include: livenessState
      probes:
        enabled: true
seata:
  enabled: true
  enable-auto-data-source-proxy: false
  application-id: ${spring.application.name}
  tx-service-group: wms_inventory_group
wms:
  oidc:
    issuer: ${WMS_OIDC_ISSUER:}
    jwk-set-uri: ${WMS_OIDC_JWK_SET_URI:}
    client-id: ${WMS_OIDC_CLIENT_ID:}
  serial:
    client:
      enabled: ${WMS_SERIAL_CLIENT_ENABLED:false}
      base-url: ${WMS_SERIAL_CLIENT_BASE_URL:}
      token-directory: ${WMS_SERIAL_CLIENT_TOKEN_DIRECTORY:}
      allow-http: ${WMS_SERIAL_CLIENT_ALLOW_HTTP:false}
  reconciliation:
    collector:
      enabled: ${WMS_RECONCILIATION_COLLECTOR_ENABLED:false}
      inbound-url: ${WMS_RECONCILIATION_INBOUND_URL:}
      outbound-url: ${WMS_RECONCILIATION_OUTBOUND_URL:}
      token-directory: ${WMS_RECONCILIATION_TOKEN_DIRECTORY:}
      allow-http: ${WMS_RECONCILIATION_ALLOW_HTTP:false}
  inventory:
    datasource:
      url: ${WMS_INVENTORY_JDBC_URL:}
      username: ${WMS_INVENTORY_DB_USER:}
      password: ${WMS_INVENTORY_DB_PASSWORD:}

logging:
  pattern:
    level: "%5p [requestId=%X{requestId:-}]"
