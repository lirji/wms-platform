<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lrj.wms.runtime.TimeProbeMapper">
    <insert id="insert">INSERT INTO time_sample (id,created_at,business_date) VALUES (#{id},#{time},'2026-11-01')</insert>
    <select id="rows" resultType="map">SELECT * FROM time_sample ORDER BY created_at DESC,id DESC LIMIT 2</select>
    <select id="after" resultType="map">
        SELECT * FROM time_sample WHERE created_at &lt; #{page.time} OR (created_at=#{page.time} AND id &lt; #{page.id})
        ORDER BY created_at DESC,id DESC LIMIT 2
    </select>
</mapper>
